void setup() {
  Serial.begin(9600); // Initialize serial communication
}

void loop() {
  int ldrValue = analogRead(A0); // Read LDR value (0-1023)
  Serial.println(ldrValue);      // Print to Serial Monitor
  delay(100);                   // Slow down updates
}
